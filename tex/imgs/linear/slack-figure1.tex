\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english, ukrainian]{babel}
\usepackage[default,scale=.90]{opensans}
\usepackage{newtxmath}
\begin{document}

\begin{tikzpicture}[
    scale=.8,
    point/.style={circle, fill, inner sep=1.5pt, black},
    axis/.style={->},
    tick/.style={thin},
    ticklabel/.style={fill=white},
    label/.style={
        fill=white,
        anchor=south west,
        yshift=2pt, xshift=1pt
    },
    legendnode/.style={
        shape=rectangle,
        minimum width= .5cm,
        minimum height= .5cm
    },
    slack_line/.style={
        semithick
    }
]

    % \draw [thick, draw=none, fill=cyan!10]
    %     (0,5) -- (2,4) -- (5,1) -- (5,0) -- (0,0) -- cycle;

    \draw[axis]
        (-.5,0) -- (8,0)
        node[anchor=south]{$x_1$};

    \draw[axis]
        (0,-.5) -- (0,8)
        node[anchor=south]{$x_2$};

    \draw[slack_line, purple]
        (0, 5) -- (7, 1.5)
        node[pos=.8, anchor=south west, rotate=-25] {$s_1 = 0$};

    \draw[slack_line, orange!50!red]
        (0, 6) -- (6, 0)
        node[pos=.5, anchor=south west, rotate=-40] {$s_2 = 0$};

    \draw[slack_line, teal]
        (5, 0) -- (5, 7)
        node[anchor=west] {$s_3 = 0$};

    \foreach \x in {1,...,7}{
        % x2 grid
        % \draw [grid] (\x, -.5) -- (\x, 5);
        \coordinate (x_tick\x) at (\x, 0);
        \coordinate (x_tick_a\x) at
            ($(x_tick\x) - (0, 2pt)$);
        \coordinate (x_tick_b\x) at
            ($(x_tick\x) + (0, 2pt)$);
        \draw [tick] (x_tick_a\x) -- (x_tick_b\x);
        \node at (x_tick\x)
            [ticklabel, yshift=-8pt] {$\x$};
    }

    \foreach \y in {1,...,7}{
        % x1 grid
        % \draw [grid] (-.5, \y) -- (5, \y);
        \coordinate (y_tick\y) at (0, \y);
        \coordinate (y_tick_a\y) at
            ($(y_tick\y) - (2pt, 0)$);
        \coordinate (y_tick_b\y) at
            ($(y_tick\y) + (2pt, 0)$);
        \draw [tick] (y_tick_a\y) -- (y_tick_b\y);
        \node at (y_tick\y)
            [ticklabel, xshift=-8pt] {$\y$};
    }

    \draw (0, 0)
        node [point] (point05) {}
        node [label, anchor=south west] {$\mathbf{X}_0$};

    \draw (0, 5)
        node [point] (point05) {}
        node [label, anchor=south west] {$\mathbf{X}_1$};

    \draw (2, 4)
        node [point] (point05) {}
        node [label, anchor=south west] {$\mathbf{X}_2$};

    \draw (5, 1)
        node [point] (point05) {}
        node [label, anchor=south west] {$\mathbf{X}_3$};

    \draw (5, 0)
        node [point] (point05) {}
        node [label, anchor=south east, xshift=-2pt] {$\mathbf{X}_4$};
        
    \matrix [draw, fill=white,below left]
    at (13, 8) {
        \node (node1) [legendnode] {}
            node [anchor=north west]
            {$a_{11} x_1 + a_{12} x_2 + \textcolor{purple}{s_1} = b_1$}; \\
        \node (node2) [legendnode] {}
            node [anchor=north west]
            {$a_{21} x_1 + a_{22} x_2 + \textcolor{orange!50!red}{s_2} = b_2$}; \\
        \node (node3) [legendnode] {}
            node [anchor=north west]
            {$a_{31} x_1 + a_{32} x_2 + \textcolor{teal}{s_3} = b_3$}; \\
    };

    \draw[slack_line, purple]
        (node1.west) -- (node1.east);

    \draw[slack_line, orange!50!red]
        (node2.west) -- (node2.east);

    \draw[slack_line, teal]
        (node3.west) -- (node3.east);

\end{tikzpicture}

\end{document}
